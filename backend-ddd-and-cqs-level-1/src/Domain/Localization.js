"use strict";

class Localization {
    constructor(latitude, longitude) {
        this.latitude = latitude; // Coordinates GPS - Latitude
        this.longitude = longitude; // Coordinates GPS - Longitude
    }
}

module.exports = Localization;
